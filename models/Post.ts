export interface IProduct {
    addedOn: string
    category?: string
    history: number[]
    id: string
    isActive: boolean
    maxPrice: number
    minPrice: number
    price?: number
    title: string
    url: string
}